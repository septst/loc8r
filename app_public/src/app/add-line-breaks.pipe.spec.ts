import { AddLineBreaksPipe } from './add-line-breaks.pipe';

describe('AddLineBreaksPipe', () => {
  it('create an instance', () => {
    const pipe = new AddLineBreaksPipe();
    expect(pipe).toBeTruthy();
  });
});
